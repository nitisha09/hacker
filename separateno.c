#include<stdio.h>
int main()
{
   int q;char s[32];
   scanf("%d",&q);
   while(q-->0)
   {
       scanf("%s",s);
       for(i=0;i<strlen(s);i++)
       {
           if(s[i+1]-s[i]!=0)
              if()

       }
   }
   return 0;
}
